import 'package:flutter/widgets.dart';

class ProfileTeacherMenuView extends StatelessWidget {
  const ProfileTeacherMenuView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [Text("This is menu view")],
      ),
    );
  }
}
